# Production environment variables for Vercel deployment.
# API URL for the Fly.io-hosted backend.
# Update this once `flyctl deploy` from apps/api produces a final domain.
VITE_API_URL=https://salon-hub-api.fly.dev/api
